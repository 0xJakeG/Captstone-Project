Hey team here are a few things you might want to know before getting started

#1. Live Server will not work for Node.js
    In order to run a test server you need to do the following
        - Make sure you have Node.js installed
        - run the following node command in termanal (npm run build)
        - After this fun the following script (serve -s build) 
        -NOTE: For some reason you are not allowed to run this script by 
               default in powershell you can enable it by following this
               stack overflow post https://stackoverflow.com/questions/56199111/visual-studio-code-cmd-error-cannot-be-loaded-because-running-scripts-is-disabl
               or you can just use cmd for your terminal you can enable
               this by using the drop down next to the + symbol in the 
               bottom right