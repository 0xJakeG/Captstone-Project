<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('./partials/head'); %>
  <title><%= recipe.recipe_name %></title>
</head>
<body>
  <header>
    <%- include('./partials/header'); %>
  </header>

  <main>
    <section>
      <h1><%= recipe.recipe_name %></h1>
      <img src="<%= recipe.recipe_picture %>" alt="Recipe picture">
      <h2>Description</h2>
      <p><%= recipe.recipe_description %></p>

      <h2>Ingredients</h2>
        <ul>
          <% ingredients.forEach((ingredient) => { %>
            <li><%= ingredient.measurement_qty %> <%= ingredient.measurement_unit %>: <%= ingredient.ingredient_name %></li>
          <% }) %>
        </ul>

     
        <h2>Instructions</h2>
        <ul>
          <% instructions.forEach((instruction) => { %>
            <li><%= instruction.order_number %>: <%= instruction.instruction_description %></li>
          <% }) %>
        </ul>
        

    </section>
  </main>

</body>
</html>